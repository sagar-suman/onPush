import { RunOutsideAngularPipe } from './run-outside-angular.pipe';

describe('RunOutsideAngularPipe', () => {
  it('create an instance', () => {
    const pipe = new RunOutsideAngularPipe();
    expect(pipe).toBeTruthy();
  });
});
